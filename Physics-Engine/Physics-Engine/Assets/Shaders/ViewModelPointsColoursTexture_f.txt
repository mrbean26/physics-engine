#version 330 core

in vec2 textureCoords;
in vec3 vertexColor;

uniform sampler2D texture0;
uniform vec3 colour;

out vec4 FragColor;

void main(){
	FragColor = texture(texture0, textureCoords) * vec4(colour, 1.0f) * vec4(vertexColor, 1.0f);
}